const Max_itens = 9;
const Max_left = (Max_itens - 1) / 2;
const pagination = ({ limit, total, offset }) => {
  const paginaAtual = offset / limit;
  
    return (
    <div>
      <label htmlFor="">ola</label>
    </div>
  );
};
export default pagination;
